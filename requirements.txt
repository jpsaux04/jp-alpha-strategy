# ── Direct dependencies (floors only) ────────────────────────────────────────
# For a reproducible install use the fully pinned transitive set instead:
#     venv/bin/pip install -r requirements.lock
#
# Python: 3.12.3   (venv at ./venv — the system python has no packages)

# Market data
yfinance>=1.4.1

# Numerics / data frames
pandas>=3.0.3
numpy>=2.4.6

# Broker REST client (Alpaca is called over plain HTTP)
requests>=2.34.2

# Loads APCA_* credentials from .env — never hardcode keys
python-dotenv>=1.2.2

# live_server.py dashboard host
flask>=3.1.3
gunicorn>=26.0.0

# make_tearsheet.py / backtest plots (was an undeclared direct import)
matplotlib>=3.11.1
