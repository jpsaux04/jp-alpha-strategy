# Secrets — NEVER commit
.env

# Python
venv/
__pycache__/
*.pyc

# Runtime data (local state, not code)
state.json
logs/
cron.log

# Track record CSVs are generated data — keep them out of git
# (comment these two lines if you WANT the history versioned)
equity_curve.csv
trade_log.csv

# Monitoring artifacts (Phase 0) — generated at runtime, not code
trades_closed.csv
positions_history.csv
heartbeat.json
dashboard.html
monitor_status.json
alerts.log

# Rollback backups
.phase*_backup/

# OS
.DS_Store

# Local patch scripts & pre-change backups (working artifacts, not source)
*.pre_*
*.bak.*
.crontab.bak.*
patch_*.py

# Research run artifacts (regenerable from backtest.py + manifest).
# Keep the repo reviewable: only curated results are committed.
a_*_equity.csv
a_*_trades.csv
a_*_results.json
v_*_equity.csv
v_*_trades.csv
v_*_results.json
wf_*_equity.csv
wf_*_trades.csv
wf_*_results.json
bt_regress_*
backtest_ext_*
p0_instr_*
data/
f_*
lo_oos*
s2_*
